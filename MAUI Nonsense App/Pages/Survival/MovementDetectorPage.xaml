<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="MAUI_Nonsense_App.Pages.Survival.MovementDetectorPage"
             Title="MovementDetectorPage">
    
    <VerticalStackLayout Padding="20" Spacing="20">
        <Label Text="Configure Movement Alarm"
               FontSize="20"
               HorizontalOptions="Center" />

        <Label Text="Arming Delay (seconds)" />
        <Picker ItemsSource="{Binding AvailableDelays}"
                SelectedItem="{Binding SelectedDelay}"
                Title="Select delay" />

        <Label Text="Sensitivity" />
        <Picker ItemsSource="{Binding AvailableSensitivities}"
                SelectedItem="{Binding SelectedSensitivity}"
                Title="Select sensitivity" />

        <Button Text="{Binding ArmButtonText}" Command="{Binding ToggleAlarmCommand}" IsEnabled="{Binding IsArmButtonEnabled}" />

        <Label Text="{Binding StatusMessage}"
               HorizontalOptions="Center"
               TextColor="DarkGreen"
               FontSize="16" />
    </VerticalStackLayout>
</ContentPage>